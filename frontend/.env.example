# Base URL for the EduSense API.
# Leave unset in development: Vite proxies /api to http://localhost:5001 (see vite.config.js).
# In production set this to the deployed API origin, for example:
#   VITE_API_BASE_URL=https://edusense-api.onrender.com/api/v1
VITE_API_BASE_URL=
